## shield-http

  - code
  - error 
  - cache
  - loading
  - queue



### Code 

code定义

    code =[
      {
        val: 'val',
        message: "msg"
      }
    ]





# Auth 需要的基础类



  ```
  
  export let getToken = function () {
  
  }
  export let setToken = function (token) {
  
  }
  export let clear = function () {
   
  }
  
  ```
